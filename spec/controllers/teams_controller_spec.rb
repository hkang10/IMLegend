require 'rails_helper'

describe TeamsController do
  # let!(:team) { Team.create!(team_name: 'wolves', teacher_id: 1, captain_id: 1) }

  # describe "GET #show" do
  #   it "responds with status code 200" do
  #     session[:user_id] = 1
  #     get :show, { id: team.id}
  #     expect(response).to have_http_status 200
  #   end

  #   it "assigns the current team as @team" do
  #     session[:user_id] = 1
  #     get :show, { id: team.id}
  #     expect(assigns(:team)).to eq(team)
  #   end

  # end
end
